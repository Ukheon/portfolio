export const theme = {
    width: {
        desktop: '900px',
        mobile: '500px',
    },
    height: {
        header: '4.5rem',
    },
};

export type width = typeof theme.width;
export type height = typeof theme.height;
